<launch>
    <arg name="config_path" default="$(find arm_common)/config/rviz_default_config.rviz"/>

    <!-- rviz节点 -->
    <node
            pkg="rviz"
            type="rviz"
            name="rviz"
            required="true"
            args="-d '$(arg config_path)'"
    />
</launch>
